---
import { Image } from "astro:assets";
import type { Project } from "../types/config";

type Props = Project;

const { title, description, image, year, url, skills } = Astro.props;
---

<a
  href={url}
  target="_blank"
  class="sm:py-5 py-10 flex flex-col sm:flex-row gap-6 
         group-hover:opacity-60 hover:!opacity-100 
         transition-all duration-300 transform hover:-translate-y-1 
         bg-glass backdrop-glass rounded-xl p-6 
         border border-white/10 hover:border-white/20"
>
  <div class="h-full bg-gray-700 rounded-lg aspect-video sm:h-[150px]">
    <Image
      width={1920}
      height={1080}
      src={image}
      alt={title}
      class="w-full h-full object-cover rounded-lg aspect-video object-top"
    />
  </div>
  <div>
    <div class="mb-3">
      <h2 class="font-medium mb-1">{title}</h2>
      <p class="text-sm opacity-60 mb-2">{year}</p>
      <div class="flex flex-wrap gap-2">
        {skills.split(" · ").map((skill) => (
          <span class="text-xs bg-blue-500/10 text-blue-400 px-2 py-1 rounded-full">
            {skill}
          </span>
        ))}
      </div>
    </div>
    <div class="opacity-60">
      {description}
    </div>
  </div>
</a>
